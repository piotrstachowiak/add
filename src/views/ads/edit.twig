{% extends 'base.twig' %}

{% block title %}
    {{ 'Edit ad'|trans }}
{% endblock %}

{% block content %}
    <form action="{{ url('ads_edit', {id: id}) }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" value="{{ 'Save'|trans }}"/>
    </form>
{% endblock %}